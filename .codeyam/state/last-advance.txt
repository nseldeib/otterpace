━━━ BEGIN STEP 25: Feature Complete (FINAL STEP) ━━━

NEXT ACTION (same turn — do not stop):

  EXACT_TASK_TITLE: Complete codeyam editor step 25: 'Feature Complete' — final step

# UI Flow 25 / 25: Feature Complete

**Feature:** Accessible & Responsive Design — Contrast, Dynamic Type, Layout

Fire the feature-complete modal — the cycle ends here.

## Instructions

RESUMING this step. Check what was already done before repeating work.
  (Resuming in a fresh session? TaskList may be empty — the previous-step task lived in the prior session's in-memory store. Skip the close-prior step; just create THIS step's task and continue.)

Fire the feature-complete modal. The cycle ends here.

Checklist:
[ ] Run `codeyam-editor-dev editor feature-complete`. The command verifies a clean
    tree, that the branch was pushed (or auto-skips if no remote), and POSTs
    the completion signal to the editor backend. Success writes a durable
    `featureCompleteSignaledAt` timestamp into `.codeyam/feature-finalized.json`.
[ ] If the command fails, read the error and fix the underlying issue
    (uncommitted changes, unpushed branch, etc.). Do NOT bypass — the
    completion modal must fire before the cycle is considered done.
[ ] Once the modal has fired, the workflow is over. Any new user request
    — even if it sounds related — is a NEW feature. Do NOT use
    `codeyam-editor-dev editor change` or start building directly. The change
    workflow is ONLY for modifications during the pre-commit slugs, not after
    the modal fires. Ask the user what they want to build. Once they tell you,
    run `codeyam-editor-dev editor step --slug ui-plan --mode ui` (or
    `--slug backend-plan --mode backend`) yourself.
[ ] NEVER tell the user to run `codeyam-editor-dev editor` commands — these are
    internal. Just ask what to build.

━━━ DO NOT OFFER TO /SCHEDULE ━━━
The harness offers a `/schedule` skill that asks the model to proactively
offer recurring or follow-up agents at the end of work. That offer is
explicitly OUT OF SCOPE here.
- Do NOT offer to /schedule anything from this closing turn.
- Do NOT end your reply with "Want me to /schedule an agent…"
- Do NOT suggest a recurring sweep, a follow-up PR cleanup, or any
  background routine.
- The editor workflow ends with the completion modal. The user fires
  the next cycle by typing what they want to build next.
- Recurring/follow-up offers belong to the harness, not to the editor
  workflow's closing turn.

This is the final step. There is no advance.


Run `codeyam-editor-dev editor feature-complete` to show the completion modal, then ask what to build next.

━━━ TASK ━━━
If TaskList/TaskCreate/TaskUpdate are not loaded in this session, call `ToolSearch` with `select:TaskList,TaskCreate,TaskUpdate` first.

Hand off from the previous step's task to this step's task. Procedure:
  1. Run TaskList
  2. If a task titled exactly `Complete codeyam editor step 24: 'Push' and move on to step 25: 'Feature Complete'` exists and is not yet completed, run TaskUpdate with status "completed" on it
  3. Run TaskCreate with the EXACT title below and set it to in_progress:

EXACT_TASK_TITLE: Complete codeyam editor step 25: 'Feature Complete' — final step

This is the final step. When you finish its checklist, mark this task completed — there is no next step to hand off to.
Do NOT use status "deleted" on any step task — tasks are check-off records of completed work.

━━━ FEATURE COMPLETE ━━━

Do NOT offer to /schedule a follow-up agent.
Do NOT end your reply with a scheduling pitch — the harness's
/schedule skill description does not apply here.
The cycle ends with the completion modal; the user
fires the next cycle by telling you what to build next.

Do NOT write a recap. Do NOT call `codeyam-editor-dev editor advance` —
this is the final step. There is no next step.

━━━ BEGIN STEP 25: Feature Complete (FINAL STEP) ━━━

NEXT ACTION (same turn — do not stop):

  EXACT_TASK_TITLE: Complete codeyam editor step 25: 'Feature Complete' — final step

  1. TaskCreate with the EXACT title above, status in_progress
  2. Run the step 25 checklist (printed above this trailer)
  3. TaskUpdate completed
  4. Run `codeyam-editor editor feature-complete`

Tail-truncated this output? Re-run without piping: codeyam-editor editor advance
